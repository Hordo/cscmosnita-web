# Django Backend API (Koyeb in production, localhost:8000 in local dev)
VITE_API_URL=http://localhost:8000

# VAPID public key for Web Push notifications
VITE_VAPID_PUBLIC_KEY=your_vapid_public_key_here
