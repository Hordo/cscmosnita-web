[deploy]
startCommand = "gunicorn backend.wsgi"
preDeployCommand = "python manage.py collectstatic --noinput && python manage.py migrate && python manage.py shell -c \"from django.contrib.auth import get_user_model; User = get_user_model(); User.objects.filter(username='hordo').exists() or User.objects.create_superuser('hordo', 'bogdan.hordoban@yahoo.com', 'changeme')\""
